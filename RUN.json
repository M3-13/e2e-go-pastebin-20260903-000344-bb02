{
  "runcontract": 1,
  "services": [
    {
      "name": "pastebin-api",
      "dir": ".",
      "stack": "go",
      "kind": "server",
      "start": ["go", "run", "."],
      "port": 8080,
      "port_env": "PORT",
      "health": { "path": "/health", "expect_status": [200] },
      "env": {
        "CERT_FILE": { "external": true, "degraded_without": "TLS and HSTS are disabled; the server serves plain HTTP, intended behind a TLS-terminating reverse proxy" },
        "KEY_FILE": { "external": true, "degraded_without": "TLS and HSTS are disabled; the server serves plain HTTP, intended behind a TLS-terminating reverse proxy" },
        "CORS_ALLOWED_ORIGINS": { "external": true, "degraded_without": "no Access-Control-Allow-Origin header is emitted; cross-origin requests are denied by default" }
      }
    }
  ]
}
